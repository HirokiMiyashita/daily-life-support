services:
  api:
    build:
      context: ../apps/api
      dockerfile: Dockerfile
    env_file:
      - path: ../apps/api/.env
        required: false
    ports:
      - "8080:8080"
    restart: unless-stopped
